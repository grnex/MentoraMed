services:
  app:
    build:
      context: .
      args:
        VITE_GEMINI_API_KEY: ${VITE_GEMINI_API_KEY}
    env_file:
      - .env
    environment:
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY}
    ports:
      - "8080:80"
    restart: always
    container_name: mentoramed-app
